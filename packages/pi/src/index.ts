// @cortexkit/pi-antigravity-auth
// pi extension entry point. Implemented in Stage 3.
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent"

export default function cortexKitPiAntigravityAuth(_pi: ExtensionAPI): void {
  // Provider registration added in Stage 3.
}
